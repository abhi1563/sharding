DELIMITER $$

CREATE PROCEDURE insert_dummy_users()
BEGIN
    DECLARE i INT DEFAULT 1;
    WHILE i <= 10000 DO
        INSERT INTO users (name, country) 
        VALUES (CONCAT('User', i), IF(i % 2 = 0, 'India', 'USA'));
        SET i = i + 1;
    END WHILE;
END $$

DELIMITER ;
